(define (a)
  (<change> (b) #t)
  (d))
(define (b)
  (c)
  (a)
  (e))
(define (c)
  (<change> (b) #t)
  (d)
  (<change> (e) #t))
(define (d)
  (<change> (e) #t))
(define (e)
  (<change> #t (d))
  (b))
(a)